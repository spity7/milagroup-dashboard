version: '3.8'

services:
  client:
    build:
      context: ./client
    ports:
      - "3011:80"
    restart: always

  server:
    build:
      context: ./server
    ports:
      - "5011:5011"
    env_file:
      - ./server/.env
    restart: always
